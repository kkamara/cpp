#include <iostream>

int main() {
  /*
    A pointer that doesn't point to a valid memory address. Trying to
    dereference and use a dangling pointer will result in undefined behaviour
    - uninitialized pointer
    - deleted pointer
    - multiple pointers pointing to same memory.
  */

  return 0;
}