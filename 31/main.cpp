#include <iostream>

int main() {
  /* references:
      - don't use dereferencing for reading and writing
      - can't be changed to reference something else
      - must be initialized at declaration
     pointers:
      - must go through dereference operator to read / 
        write through pointed to value
      - can be changed to point somewhere else
      - can be declared un-initialized (will
        contain garbage addresses).
  */

  return 0;
}